# Configuración de base de datos PostgreSQL
# Renombra este archivo como .env.local y modifica los valores según tu configuración

# Usuario de PostgreSQL
DB_USER=postgres

# Host de la base de datos (localhost para desarrollo local)
DB_HOST=localhost

# Nombre de la base de datos
DB_DATABASE=cobit

# Contraseña del usuario PostgreSQL
DB_PASSWORD=your_password_here

# Puerto de PostgreSQL (5432 es el puerto por defecto)
DB_PORT=5432

# Instrucciones:
# 1. Renombra este archivo: env.example → .env.local
# 2. Edita .env.local con tus credenciales reales
# 3. El archivo .env.local nunca se sube a Git por seguridad
# 4. Puedes subir este archivo env.example a Git sin problemas
